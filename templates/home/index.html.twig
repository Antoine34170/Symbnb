{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur le meilleur réseau d'appartement du monde
{% endblock %}

{% block body %}
	<div class="container my-5">


		<div class="jumbotron">
			<h1 class="display-4">Bienvenue sur SYMBNB</h1>
			<p class="lead">Nous mettons en relation les voyageurs et les propriétaires qui veulent profiter d'opportunité et gagner de l'argent</p>
			<hr class="my-4">
			<p>Vous êtes voyageurs et vous voulez voir où vous pourrez loger pour votre déplacement.</p>
			<a class="btn btn-primary btn-lg" href="#" role="button">Voir vos annonces</a>
		</div>

		<h3 class="mt-5">
			Nos appartements stars</h3>
		<div class="row mt-5">

			{% for data in bestAds %}
				{% include "ad/_ad.html.twig" with {'ad': data.annonce}%}
			{% endfor %}


		</div>

		<h2 class="my-5">Nos propriétaires stars !</h2>
		<div class="row">
			{% for data in bestUsers %}
				<div class="col-md-6">
					<div class="card border-secondary mb-3">
						<div class="card-body">
							<h4 class="card-title">{{data.user.fullName}}</h4>
							<hr>
							<div class="card-text">

								<img src="{{data.user.picture}}" alt="Avatar de {{data.user.fullName}}" class="float-start me-3">
								<p> {{data.user.introduction}}</p>
								<a href="{{path('user_show', {'slug': data.user.slug})}}" class="btn btn-success float-end">En savoir plus</a>
							</div>
						</div>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>
{% endblock %}
